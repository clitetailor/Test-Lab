#include <iostream.h>
#include <sstream>
using namespace std;

void mergesort(int * f, int n)
{
	
}

int main()
{
	int f[argc];
	for (int i = 1; i < argc; i++)
	{
		istringstream(argv[i]) >> f[i - 1];
	}
	
	mergesort(f, argc - 1);
}